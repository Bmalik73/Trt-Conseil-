{% extends 'base.html.twig' %}

{% block title %}ACCUEIL :: TRT Conseil{% endblock %}

{% block body %}
    
    <div class="w-100">
        <div class="text-center">
            <div class="middle-window position-absolute top-50 start-50 translate-middle">
                <h1 class="h1 m-5">TRT Conseil</h1>
                <div>
                    <a class="btn btn-primary button-shadow me-2" href="{{ path('app_login') }}" role="button">Se connecter</a>
                    <a class="btn btn-light button-shadow ms-2" href="{{ path('app_register') }}" role="button">Créer un compte</a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
