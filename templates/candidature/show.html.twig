{% extends 'base.html.twig' %}

{% block title %}Candidatures{% endblock %}

{% block body %}
    <h1>Candidatures</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Prénom</th>
                <td>{{ candidature.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ candidature.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_candidature_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('app_candidature_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('candidature/_delete_form.html.twig') }}
{% endblock %}
